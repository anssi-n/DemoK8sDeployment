apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: bar
  name: second-app
  labels:
    app: second-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: second-app
  template:
    metadata:
      labels:
        app: second-app
    spec:
      containers:
        - name: secondapp
          image: bobfleming/k8s-demo-app:1.0
          ports:
            - containerPort: 80
          env:
            - name: "APP_NAME"
              value: "second-app"
            - name: "APP_VERSION"
              value: "1.0"
---
apiVersion: v1
kind: Service
metadata:
  namespace: app
  name: second-app
spec:
  selector:
    app: second-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
